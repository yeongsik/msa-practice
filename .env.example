# Docker Compose 환경변수 템플릿
# ✅ 이 파일은 Git에 커밋됩니다 (실제 값은 포함하지 않음)
#
# 사용법:
# 1. 이 파일을 .env로 복사: cp .env.example .env
# 2. .env 파일의 값을 실제 값으로 변경

# User Service MySQL
USER_MYSQL_ROOT_PASSWORD=your_password_here
USER_MYSQL_DATABASE=userdb

# Board Service MySQL
BOARD_MYSQL_ROOT_PASSWORD=your_password_here
BOARD_MYSQL_DATABASE=boarddb

# MySQL 공통 설정
MYSQL_CHARSET=utf8mb4
MYSQL_COLLATION=utf8mb4_unicode_ci