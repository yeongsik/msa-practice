# Jenkins vs GitHub Actions 비교 가이드

## 🎯 결론부터 (현재 프로젝트 기준)

### 추천: **GitHub Actions** ✅

**이유**:
- ✅ 개인 학습/포트폴리오 프로젝트
- ✅ GitHub 사용 중
- ✅ 서버 관리 부담 없음
- ✅ 빠른 시작 가능
- ✅ 무료 (Public repo)

### Jenkins는 언제 고려할까? 🤔

- 대규모 엔터프라이즈 환경
- 복잡한 파이프라인 (100+ 단계)
- 온프레미스 환경 필수
- 비용 제약 (매우 많은 빌드)

---

## 📊 상세 비교

### 1. 설치 및 설정

#### GitHub Actions
```yaml
# .github/workflows/ci.yml 생성만 하면 끝!
name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: ./gradlew build
```

**소요 시간**: 5분
**난이도**: ⭐ (매우 쉬움)

#### Jenkins
```groovy
// 1. Jenkins 서버 설치 (30분)
docker-compose up -d jenkins

// 2. 플러그인 설치 (10분)
- Git Plugin
- Docker Plugin
- Gradle Plugin
- ... (10개 이상)

// 3. Jenkinsfile 작성
pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        sh './gradlew build'
      }
    }
  }
}

// 4. GitHub Webhook 설정
// 5. Credentials 설정
// ...
```

**소요 시간**: 1-2시간
**난이도**: ⭐⭐⭐⭐ (어려움)

---

### 2. 비용

#### GitHub Actions

| 타입 | 무료 제공량 | 초과 시 |
|------|------------|---------|
| **Public Repo** | 무제한 | - |
| **Private Repo** | 월 2000분 | $0.008/분 |

**예시 계산**:
- 빌드 시간: 5분
- 일 빌드 횟수: 10회
- 월 사용량: 5분 × 10회 × 30일 = **1500분** (무료)

#### Jenkins

| 항목 | 비용 |
|------|------|
| **소프트웨어** | 무료 |
| **서버 비용** | AWS t3.medium: $30/월<br>전기세 (자체 서버): 변동 |
| **유지보수** | 엔지니어 시간 |

**총 비용**: 월 $30+ (서버만)

---

### 3. 기능 비교

| 기능 | GitHub Actions | Jenkins |
|------|---------------|---------|
| **병렬 실행** | ✅ (Matrix) | ✅ (Parallel) |
| **캐싱** | ✅ | ✅ (플러그인) |
| **Docker 지원** | ✅ (네이티브) | ✅ (플러그인) |
| **멀티 브랜치** | ✅ | ✅ |
| **승인 단계** | ✅ (Environment) | ✅ (Input) |
| **조건부 실행** | ✅ | ✅ |
| **플러그인** | 제한적 (Marketplace) | 1800+ 플러그인 |
| **커스터마이징** | 제한적 | 무제한 |

---

### 4. 실제 사용 예시

#### Scenario: User Service 빌드

**GitHub Actions (ci.yml)**:
```yaml
name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          cache: 'gradle'
      - run: ./gradlew :user-service:build
```
**라인 수**: 11줄
**이해하기**: 쉬움

**Jenkins (Jenkinsfile)**:
```groovy
pipeline {
  agent any
  tools {
    jdk 'JDK17'
    gradle 'Gradle8'
  }
  stages {
    stage('Build') {
      steps {
        sh './gradlew :user-service:build'
      }
    }
  }
  post {
    always {
      cleanWs()
    }
  }
}
```
**라인 수**: 18줄
**이해하기**: 중간

---

### 5. 학습 곡선

```
난이도
  ↑
  │
5 │                    Jenkins (Groovy, 플러그인)
  │
4 │
  │
3 │    GitHub Actions (YAML)
  │
2 │
  │
1 │
  └─────────────────────────────────────────→ 시간
    1주    2주    3주    4주    8주
```

#### GitHub Actions 학습 경로
```
1주차: YAML 기본 문법
2주차: 간단한 CI 구축
3주차: Docker 빌드
4주차: CD 배포
```

#### Jenkins 학습 경로
```
1주차: Jenkins 설치 및 설정
2주차: 플러그인 이해
3주차: Groovy 문법
4주차: 파이프라인 기본
8주차: 고급 파이프라인
```

---

### 6. 실무 사용 통계

#### 2024년 CI/CD 도구 사용률

```
GitHub Actions  ████████████████░░░░  65% (증가 추세)
Jenkins         ███████████████░░░░░  60% (유지)
GitLab CI       ████████░░░░░░░░░░░░  35%
CircleCI        █████░░░░░░░░░░░░░░░  20%
```
*출처: Stack Overflow Developer Survey 2024*

**트렌드**:
- GitHub Actions: 📈 급성장
- Jenkins: 📊 안정적 (레거시 시스템)

---

### 7. 장단점 요약

#### GitHub Actions

**장점** ✅:
- 설치 불필요
- GitHub와 완벽한 통합
- 간단한 YAML 설정
- 빠른 시작 (5분)
- 자동 스케일링
- 보안 업데이트 자동

**단점** ❌:
- 복잡한 파이프라인 제한
- 플러그인 생태계 작음
- 비용 (Private repo 많은 빌드 시)
- GitHub 종속

#### Jenkins

**장점** ✅:
- 완전 무료 (서버만 있으면)
- 무한한 커스터마이징
- 1800+ 플러그인
- 복잡한 파이프라인 가능
- 다양한 VCS 지원
- 온프레미스 가능

**단점** ❌:
- 서버 설치/관리 필요
- 복잡한 설정
- 학습 곡선 높음
- 보안 업데이트 직접 관리
- 초기 설정 시간 오래 걸림

---

## 🎓 학습 시나리오별 추천

### 시나리오 1: CI/CD 처음 배우는 초보자
**추천**: GitHub Actions ⭐⭐⭐⭐⭐
```
- 빠르게 결과 확인
- 쉬운 YAML 문법
- 풍부한 예제
```

### 시나리오 2: 포트폴리오 프로젝트
**추천**: GitHub Actions ⭐⭐⭐⭐⭐
```
- GitHub과 자연스러운 통합
- 면접관이 쉽게 확인 가능
- 최신 트렌드 반영
```

### 시나리오 3: 실무 경험 쌓기
**추천**: 둘 다 ⭐⭐⭐⭐
```
- GitHub Actions: 먼저 시작 (쉬움)
- Jenkins: 추가 학습 (차별화)
```

### 시나리오 4: 취업 준비
**추천**: GitHub Actions 우선, Jenkins 보조
```
- GitHub Actions: 대부분 스타트업
- Jenkins: 대기업/금융권
```

---

## 🚀 현재 프로젝트 권장 전략

### Phase 1: GitHub Actions로 시작 (지금)
```bash
✅ 이미 구축됨 (.github/workflows/)
✅ 바로 사용 가능
✅ 학습 및 포트폴리오에 최적
```

### Phase 2: Jenkins 학습 (선택, 나중에)
```bash
⏭️ Jenkins 기본 개념 학습
⏭️ 로컬 Docker로 Jenkins 실행
⏭️ 동일한 파이프라인 구현
⏭️ 이력서에 "Jenkins 경험" 추가
```

---

## 💻 실습: Jenkins 빠르게 체험하기

### 1. Jenkins 서버 실행
```bash
cd jenkins/
docker-compose -f docker-compose.jenkins.yml up -d

# Jenkins UI 접속
open http://localhost:8080

# 초기 비밀번호 확인
docker exec jenkins cat /var/jenkins_home/secrets/initialAdminPassword
```

### 2. 프로젝트 연결
```
1. New Item → Pipeline
2. Pipeline script from SCM
3. Repository URL 입력
4. Script Path: Jenkinsfile
5. Save
```

### 3. 빌드 실행
```
Build Now 클릭 → 빌드 로그 확인
```

---

## 📈 마이그레이션 가이드

### GitHub Actions → Jenkins

```yaml
# GitHub Actions (ci.yml)
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: ./gradlew build
```

```groovy
// Jenkins (Jenkinsfile)
pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        checkout scm
        sh './gradlew build'
      }
    }
  }
}
```

### Jenkins → GitHub Actions

역방향도 동일하게 간단합니다.

---

## 🎯 최종 추천

### 현재 프로젝트 (MSA Practice)

```
1순위: GitHub Actions ⭐⭐⭐⭐⭐
- 이미 구축 완료
- 빠른 시작
- 무료 (Public repo)

2순위: Jenkins (추가 학습용)
- Jenkinsfile 제공됨
- 로컬 Docker로 실행
- 실무 경험 쌓기
```

### 실무 진출 시

```
스타트업/중소기업:
→ GitHub Actions (80%)

대기업/금융권:
→ Jenkins (60%)
→ GitLab CI (30%)
→ GitHub Actions (10%)

글로벌 기업:
→ GitHub Actions (50%)
→ Jenkins (30%)
→ 기타 (20%)
```

---

## 🔗 더 알아보기

### GitHub Actions
- [공식 문서](https://docs.github.com/en/actions)
- [Marketplace](https://github.com/marketplace?type=actions)

### Jenkins
- [공식 문서](https://www.jenkins.io/doc/)
- [플러그인 목록](https://plugins.jenkins.io/)

### 비교 자료
- [Stack Overflow Survey](https://survey.stackoverflow.co/)
- [JetBrains DevOps Report](https://www.jetbrains.com/lp/devecosystem-2023/devops/)

---

## 💡 결론

**지금 시작**: GitHub Actions
**나중에 추가**: Jenkins (선택)
**둘 다 경험**: 이력서에 유리 ✅