server:
  port: 8761

spring:
  application:
    name: discovery-service

eureka:
  client:
    register-with-eureka: false # 서버 자신이 레지스트리에 등록할 필요 없음
    fetch-registry: false       # 레지스트리 정보를 가져올 필요 없음
